{
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^lint", "^test"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "clean": {}
  }
}
